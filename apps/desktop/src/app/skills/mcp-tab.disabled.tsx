import type { HermesGateway } from '@/hermes'

/** MCP configuration and authorization are owned by the remote Mini. */
export function McpTab(_props: { gateway: HermesGateway | null }) {
  return null
}
