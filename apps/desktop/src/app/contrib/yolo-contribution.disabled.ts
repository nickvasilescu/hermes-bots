import type { PaletteContribution } from '@/app/command-palette/contrib'

// Permanent approval bypass controls are owned by the Mini, not the SSH UI.
export const YOLO_PALETTE_CONTRIBUTIONS: readonly PaletteContribution[] = []
