// This is an absent capability, not a no-op handler. The selected SSH-only
// module contains no IPC channel name and accepts no URL or bridge argument.
export const registerLinkTitleIntegration = undefined
