/** Local/remote recovery paths are not valid in the strict SSH SKU. Typed SSH
 * failures are surfaced by the connection flow and gateway status instead. */
export function BootFailureOverlay() {
  return null
}
